import React from "react";
import ChatContainer from "./chats/ChatContainer";

const Chat = () => {
  return (
    <React.Fragment>
      <ChatContainer />
    </React.Fragment>
  );
};

export default Chat;
