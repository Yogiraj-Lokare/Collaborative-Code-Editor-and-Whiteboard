import React from "react";
import ChatContainer from "./ChatContainer/ChatContainer";

const Chat = () => {
  return (
    <React.Fragment>
      <ChatContainer />
    </React.Fragment>
  );
};

export default Chat;
